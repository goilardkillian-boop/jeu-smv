# Supabase — laisser vide pour utiliser le mode démo (données locales, sans backend)
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJxxxx

# Optionnel — si Mapbox à la place de Leaflet/OSM (non requis)
VITE_MAPBOX_TOKEN=

# Email (secrets des Supabase Edge Functions — à configurer via `supabase secrets set`)
RESEND_API_KEY=re_xxxx
SMTP_FROM=recrutement@smv-connect.fr
